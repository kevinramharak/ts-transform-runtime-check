
import { is } from '@lib';
import { expect } from 'chai';
import { bigint, boolean, cls, fn, number, string, symbol, unique } from '@test/values';

// TODO: write tests for ts.TypeFlags.Object and ts.ObjectFlags

export function test() {

}
